geom_text(aes(label = state.cat),na.rm = T)+#add label for the points we want to look at
theme(plot.title = element_text(hjust=0.5))+#put the title at the middle
theme_bw()+#add the theme
theme(panel.border=element_blank())
#Base on the graph, we could find that a higher Trump Support Rate comes with a lower saying discrimination prevalent. We can hava a negaive correlation here, which is -0.4386476
#state.select <- data$state[!c("CA","WY")]
data$state.cat<- factor(data$state,levels = c("CA","WY"),labels=c("California","Wyoming"))
#create a ggplot scatter plot based on X axis= discrim, Y axis = trump. We want to have a different size based on support ratem, and different transparent status based on discrim
G1<- ggplot(data, aes(y=discrim, x=trump))+geom_point()
#Add more details
G1+xlab("Trump Support Rate")+# Add X axis names "Trump Support Rate"
ylab("Seeing Discriminated Prevalent")+# Add Y axis names "Seeing Discriminated Prevalent"
labs(title="Perceptions Of Discrimination Track Closely With Voting Against Trump Correlation=-0.44")+
expand_limits(x=c(0.25,1),y=c(0.26,.5))+
scale_x_continuous(breaks=seq(0.25, 1, 0.05), labels = function(x){paste0(x*100, '%')})+# Add X axis scale from 0 to 1, break is 0.1. Since it is a rate, add % by using paste0()
scale_y_continuous(breaks=seq(0.26, 0.5, 0.02), labels = function(x){paste0(x*100, '%')})+# Add Y axis scale from 0 to 1, break is 0.1. Since it is a rate, add % by using paste0()
geom_smooth(method=lm,se=F)+ #add a tendency line
geom_text(aes(label = state.cat),na.rm = T)+#add label for the points we want to look at
theme(plot.title = element_text(hjust=0.5))+#put the title at the middle
theme_bw()+#add the theme
theme(panel.border=element_blank())
#Base on the graph, we could find that a higher Trump Support Rate comes with a lower saying discrimination prevalent. We can hava a negaive correlation here, which is -0.4386476
#state.select <- data$state[!c("CA","WY")]
data$state.cat<- factor(data$state,levels = c("CA","WY"),labels=c("California","Wyoming"))
#create a ggplot scatter plot based on X axis= discrim, Y axis = trump. We want to have a different size based on support ratem, and different transparent status based on discrim
G1<- ggplot(data, aes(y=discrim, x=trump))+geom_point()
#Add more details
G1+xlab("Trump Support Rate")+# Add X axis names "Trump Support Rate"
ylab("Seeing Discriminated Prevalent")+# Add Y axis names "Seeing Discriminated Prevalent"
labs(title="Perceptions Of Discrimination Track Closely With Voting Against Trump Correlation=-0.44")+
expand_limits(x=c(0.25,1),y=c(0.26,0.5))+
scale_x_continuous(breaks=seq(0.25, 1, 0.05), labels = function(x){paste0(x*100, '%')})+# Add X axis scale from 0 to 1, break is 0.1. Since it is a rate, add % by using paste0()
scale_y_continuous(breaks=seq(0.26, 0.5, 0.02), labels = function(x){paste0(x*100, '%')})+# Add Y axis scale from 0 to 1, break is 0.1. Since it is a rate, add % by using paste0()
geom_smooth(method=lm,se=F)+ #add a tendency line
geom_text(aes(label = state.cat),na.rm = T)+#add label for the points we want to look at
theme(plot.title = element_text(hjust=0.5))+#put the title at the middle
theme_bw()+#add the theme
theme(panel.border=element_blank())
#Base on the graph, we could find that a higher Trump Support Rate comes with a lower saying discrimination prevalent. We can hava a negaive correlation here, which is -0.4386476
#state.select <- data$state[!c("CA","WY")]
data$state.cat<- factor(data$state,levels = c("CA","WY"),labels=c("California","Wyoming"))
#create a ggplot scatter plot based on X axis= discrim, Y axis = trump. We want to have a different size based on support ratem, and different transparent status based on discrim
G1<- ggplot(data, aes(y=discrim, x=trump))+geom_point()
#Add more details
G1+xlab("Trump Support Rate")+# Add X axis names "Trump Support Rate"
ylab("Seeing Discriminated Prevalent")+# Add Y axis names "Seeing Discriminated Prevalent"
labs(title="Perceptions Of Discrimination Track Closely With Voting Against Trump Correlation=-0.44")+
scale_x_continuous(breaks=seq(0.25, 1, 0.05), limts=c(0.25, 1),labels = function(x){paste0(x*100, '%')})+# Add X axis scale from 0 to 1, break is 0.1. Since it is a rate, add % by using paste0()
scale_y_continuous(breaks=seq(0.26, 0.5, 0.02), labels = function(x){paste0(x*100, '%')})+# Add Y axis scale from 0 to 1, break is 0.1. Since it is a rate, add % by using paste0()
geom_smooth(method=lm,se=F)+ #add a tendency line
geom_text(aes(label = state.cat),na.rm = T)+#add label for the points we want to look at
theme(plot.title = element_text(hjust=0.5))+#put the title at the middle
theme_bw()+#add the theme
theme(panel.border=element_blank())
#state.select <- data$state[!c("CA","WY")]
data$state.cat<- factor(data$state,levels = c("CA","WY"),labels=c("California","Wyoming"))
#create a ggplot scatter plot based on X axis= discrim, Y axis = trump. We want to have a different size based on support ratem, and different transparent status based on discrim
G1<- ggplot(data, aes(y=discrim, x=trump))+geom_point()
#Add more details
G1+xlab("Trump Support Rate")+# Add X axis names "Trump Support Rate"
ylab("Seeing Discriminated Prevalent")+# Add Y axis names "Seeing Discriminated Prevalent"
labs(title="Perceptions Of Discrimination Track Closely With Voting Against Trump Correlation=-0.44")+
scale_x_continuous(breaks=seq(0, 1, 0.05), limts=c(0.25, 1),labels = function(x){paste0(x*100, '%')})+# Add X axis scale from 0 to 1, break is 0.1. Since it is a rate, add % by using paste0()
scale_y_continuous(breaks=seq(0.26, 0.5, 0.02), labels = function(x){paste0(x*100, '%')})+# Add Y axis scale from 0 to 1, break is 0.1. Since it is a rate, add % by using paste0()
geom_smooth(method=lm,se=F)+ #add a tendency line
geom_text(aes(label = state.cat),na.rm = T)+#add label for the points we want to look at
theme(plot.title = element_text(hjust=0.5))+#put the title at the middle
theme_bw()+#add the theme
theme(panel.border=element_blank())
#state.select <- data$state[!c("CA","WY")]
data$state.cat<- factor(data$state,levels = c("CA","WY"),labels=c("California","Wyoming"))
#create a ggplot scatter plot based on X axis= discrim, Y axis = trump. We want to have a different size based on support ratem, and different transparent status based on discrim
G1<- ggplot(data, aes(y=discrim, x=trump))+geom_point()
#Add more details
G1+xlab("Trump Support Rate")+# Add X axis names "Trump Support Rate"
ylab("Seeing Discriminated Prevalent")+# Add Y axis names "Seeing Discriminated Prevalent"
labs(title="Perceptions Of Discrimination Track Closely With Voting Against Trump Correlation=-0.44")+
scale_x_continuous(breaks=seq(0, 1, 0.05), limits=c(0.25, 1),labels = function(x){paste0(x*100, '%')})+# Add X axis scale from 0 to 1, break is 0.1. Since it is a rate, add % by using paste0()
scale_y_continuous(breaks=seq(0.26, 0.5, 0.02), labels = function(x){paste0(x*100, '%')})+# Add Y axis scale from 0 to 1, break is 0.1. Since it is a rate, add % by using paste0()
geom_smooth(method=lm,se=F)+ #add a tendency line
geom_text(aes(label = state.cat),na.rm = T)+#add label for the points we want to look at
theme(plot.title = element_text(hjust=0.5))+#put the title at the middle
theme_bw()+#add the theme
theme(panel.border=element_blank())
#Base on the graph, we could find that a higher Trump Support Rate comes with a lower saying discrimination prevalent. We can hava a negaive correlation here, which is -0.4386476
#state.select <- data$state[!c("CA","WY")]
data$state.cat<- factor(data$state,levels = c("CA","WY"),labels=c("California","Wyoming"))
#create a ggplot scatter plot based on X axis= discrim, Y axis = trump. We want to have a different size based on support ratem, and different transparent status based on discrim
G1<- ggplot(data, aes(y=discrim, x=trump))+geom_point()
#Add more details
G1+xlab("Trump Support Rate")+# Add X axis names "Trump Support Rate"
ylab("Seeing Discriminated Prevalent")+# Add Y axis names "Seeing Discriminated Prevalent"
labs(title="Perceptions Of Discrimination Track Closely With Voting Against Trump Correlation=-0.44")+
scale_x_continuous(breaks=seq(0, 1, 0.05), limits=c(0.25, 1),labels = function(x){paste0(x*100, '%')})+# Add X axis scale from 0 to 1, break is 0.1. Since it is a rate, add % by using paste0()
scale_y_continuous(breaks=seq(0, 1, 0.02), limits=c(0.26, 0.5),labels = function(x){paste0(x*100, '%')})+# Add Y axis scale from 0 to 1, break is 0.1. Since it is a rate, add % by using paste0()
geom_smooth(method=lm,se=F)+ #add a tendency line
geom_text(aes(label = state.cat),na.rm = T)+#add label for the points we want to look at
theme(plot.title = element_text(hjust=0.5))+#put the title at the middle
theme_bw()+#add the theme
theme(panel.border=element_blank())
#Base on the graph, we could find that a higher Trump Support Rate comes with a lower saying discrimination prevalent. We can hava a negaive correlation here, which is -0.4386476
#state.select <- data$state[!c("CA","WY")]
data$state.cat<- factor(data$state,levels = c("CA","WY"),labels=c("California","Wyoming"))
#create a ggplot scatter plot based on X axis= discrim, Y axis = trump. We want to have a different size based on support ratem, and different transparent status based on discrim
G1<- ggplot(data, aes(y=discrim, x=trump))+geom_point()
#Add more details
G1+xlab("Trump Support Rate")+# Add X axis names "Trump Support Rate"
ylab("Seeing Discriminated Prevalent")+# Add Y axis names "Seeing Discriminated Prevalent"
labs(title="Perceptions Of Discrimination Track Closely With Voting Against Trump Correlation=-0.44")+
scale_x_continuous(breaks=seq(0, 1, 0.05), limits=c(0.25, 1),labels = function(x){paste0(x*100, '%')})+# Add X axis scale from 0 to 1, break is 0.1. Since it is a rate, add % by using paste0()
scale_y_continuous(breaks=seq(0, 1, 0.02),labels = function(x){paste0(x*100, '%')})+# Add Y axis scale from 0 to 1, break is 0.1. Since it is a rate, add % by using paste0()
geom_smooth(method=lm,se=F)+ #add a tendency line
geom_text(aes(label = state.cat),na.rm = T)+#add label for the points we want to look at
theme(plot.title = element_text(hjust=0.5))+#put the title at the middle
theme_bw()+#add the theme
theme(panel.border=element_blank())
#Base on the graph, we could find that a higher Trump Support Rate comes with a lower saying discrimination prevalent. We can hava a negaive correlation here, which is -0.4386476
#state.select <- data$state[!c("CA","WY")]
data$state.cat<- factor(data$state,levels = c("CA","WY"),labels=c("California","Wyoming"))
#create a ggplot scatter plot based on X axis= discrim, Y axis = trump. We want to have a different size based on support ratem, and different transparent status based on discrim
G1<- ggplot(data, aes(y=discrim, x=trump))+geom_point()
#Add more details
G1+xlab("Trump Support Rate")+# Add X axis names "Trump Support Rate"
ylab("Seeing Discriminated Prevalent")+# Add Y axis names "Seeing Discriminated Prevalent"
labs(title="Perceptions Of Discrimination Track Closely With Voting Against Trump Correlation=-0.44")+
scale_x_continuous(breaks=seq(0, 1, 0.05), limits=c(0.25, 1),labels = function(x){paste0(x*100, '%')})+# Add X axis scale from 0 to 1, break is 0.1. Since it is a rate, add % by using paste0()
scale_y_continuous(breaks=seq(0, 1, 0.02),, limits=c(0.26, 0.50)labels = function(x){paste0(x*100, '%')})+# Add Y axis scale from 0 to 1, break is 0.1. Since it is a rate, add % by using paste0()
#state.select <- data$state[!c("CA","WY")]
data$state.cat<- factor(data$state,levels = c("CA","WY"),labels=c("California","Wyoming"))
#create a ggplot scatter plot based on X axis= discrim, Y axis = trump. We want to have a different size based on support ratem, and different transparent status based on discrim
G1<- ggplot(data, aes(y=discrim, x=trump))+geom_point()
#Add more details
G1+xlab("Trump Support Rate")+# Add X axis names "Trump Support Rate"
ylab("Seeing Discriminated Prevalent")+# Add Y axis names "Seeing Discriminated Prevalent"
labs(title="Perceptions Of Discrimination Track Closely With Voting Against Trump Correlation=-0.44")+
scale_x_continuous(breaks=seq(0, 1, 0.05), limits=c(0.25, 1),labels = function(x){paste0(x*100, '%')})+# Add X axis scale from 0 to 1, break is 0.1. Since it is a rate, add % by using paste0()
scale_y_continuous(breaks=seq(0, 1, 0.02),, limits=c(0.26, 0.50),labels = function(x){paste0(x*100, '%')})+# Add Y axis scale from 0 to 1, break is 0.1. Since it is a rate, add % by using paste0()
geom_smooth(method=lm,se=F)+ #add a tendency line
geom_text(aes(label = state.cat),na.rm = T)+#add label for the points we want to look at
theme(plot.title = element_text(hjust=0.5))+#put the title at the middle
theme_bw()+#add the theme
theme(panel.border=element_blank())
#Base on the graph, we could find that a higher Trump Support Rate comes with a lower saying discrimination prevalent. We can hava a negaive correlation here, which is -0.4386476
#state.select <- data$state[!c("CA","WY")]
data$state.cat<- factor(data$state,levels = c("CA","WY"),labels=c("California","Wyoming"))
#create a ggplot scatter plot based on X axis= discrim, Y axis = trump. We want to have a different size based on support ratem, and different transparent status based on discrim
G1<- ggplot(data, aes(y=discrim, x=trump))+geom_point()
#Add more details
G1+xlab("Trump Support Rate")+# Add X axis names "Trump Support Rate"
ylab("Seeing Discriminated Prevalent")+# Add Y axis names "Seeing Discriminated Prevalent"
labs(title="Perceptions Of Discrimination Track Closely With Voting Against Trump Correlation=-0.44")+
scale_x_continuous(breaks=seq(0, 1, 0.05), limits=c(0.25, 1),labels = function(x){paste0(x*100, '%')})+# Add X axis scale from 0 to 1, break is 0.1. Since it is a rate, add % by using paste0()
scale_y_continuous(breaks=seq(0, 1, 0.02),limits=c(0.26, 0.50),labels = function(x){paste0(x*100, '%')})+# Add Y axis scale from 0 to 1, break is 0.1. Since it is a rate, add % by using paste0()
geom_smooth(method=lm,se=F)+ #add a tendency line
geom_text(aes(label = state.cat),na.rm = T)+#add label for the points we want to look at
theme(plot.title = element_text(hjust=0.5))+#put the title at the middle
theme_bw()+#add the theme
theme(panel.border=element_blank())
#Base on the graph, we could find that a higher Trump Support Rate comes with a lower saying discrimination prevalent. We can hava a negaive correlation here, which is -0.4386476
?geom_smooth(method=lm,se=F)
#state.select <- data$state[!c("CA","WY")]
data$state.cat<- factor(data$state,levels = c("CA","WY"),labels=c("California","Wyoming"))
#create a ggplot scatter plot based on X axis= discrim, Y axis = trump. We want to have a different size based on support ratem, and different transparent status based on discrim
G1<- ggplot(data, aes(y=discrim, x=trump))+geom_point()
#Add more details
G1+xlab("Trump Support Rate")+# Add X axis names "Trump Support Rate"
ylab("Seeing Discriminated Prevalent")+# Add Y axis names "Seeing Discriminated Prevalent"
labs(title="Perceptions Of Discrimination Track Closely With Voting Against Trump Correlation=-0.44")+
scale_x_continuous(breaks=seq(0, 1, 0.05), limits=c(0.25, 0.75),labels = function(x){paste0(x*100, '%')})+# Add X axis scale from 0 to 1, break is 0.1. Since it is a rate, add % by using paste0()
scale_y_continuous(breaks=seq(0, 1, 0.02),limits=c(0.26, 0.50),labels = function(x){paste0(x*100, '%')})+# Add Y axis scale from 0 to 1, break is 0.1. Since it is a rate, add % by using paste0()
geom_smooth(method=lm,se=F)+ #add a tendency line
geom_text(aes(label = state.cat),na.rm = T)+#add label for the points we want to look at
theme(plot.title = element_text(hjust=0.5))+#put the title at the middle
theme_bw()+#add the theme
theme(panel.border=element_blank())
#Base on the graph, we could find that a higher Trump Support Rate comes with a lower saying discrimination prevalent. We can hava a negaive correlation here, which is -0.4386476
#state.select <- data$state[!c("CA","WY")]
data$state.cat<- factor(data$state,levels = c("CA","WY"),labels=c("California","Wyoming"))
#create a ggplot scatter plot based on X axis= discrim, Y axis = trump. We want to have a different size based on support ratem, and different transparent status based on discrim
G1<- ggplot(data, aes(y=discrim, x=trump))+geom_point(aes(color=state))
#Add more details
G1+xlab("Trump Support Rate")+# Add X axis names "Trump Support Rate"
ylab("Seeing Discriminated Prevalent")+# Add Y axis names "Seeing Discriminated Prevalent"
labs(title="Perceptions Of Discrimination Track Closely With Voting Against Trump Correlation=-0.44")+
scale_x_continuous(breaks=seq(0, 1, 0.05), limits=c(0.25, 0.75),labels = function(x){paste0(x*100, '%')})+# Add X axis scale from 0 to 1, break is 0.1. Since it is a rate, add % by using paste0()
scale_y_continuous(breaks=seq(0, 1, 0.02),limits=c(0.26, 0.50),labels = function(x){paste0(x*100, '%')})+# Add Y axis scale from 0 to 1, break is 0.1. Since it is a rate, add % by using paste0()
geom_smooth(method=lm,se=F)+ #add a tendency line
geom_text(aes(label = state.cat),na.rm = T)+#add label for the points we want to look at
theme(plot.title = element_text(hjust=0.5))+#put the title at the middle
theme_bw()+#add the theme
theme(panel.border=element_blank())
#Base on the graph, we could find that a higher Trump Support Rate comes with a lower saying discrimination prevalent. We can hava a negaive correlation here, which is -0.4386476
#state.select <- data$state[!c("CA","WY")]
data$state.cat<- factor(data$state,levels = c("CA","WY"),labels=c("California","Wyoming"))
#create a ggplot scatter plot based on X axis= discrim, Y axis = trump. We want to have a different size based on support ratem, and different transparent status based on discrim
G1<- ggplot(data, aes(y=discrim, x=trump))+geom_point(aes(color="blue"))
#Add more details
G1+xlab("Trump Support Rate")+# Add X axis names "Trump Support Rate"
ylab("Seeing Discriminated Prevalent")+# Add Y axis names "Seeing Discriminated Prevalent"
labs(title="Perceptions Of Discrimination Track Closely With Voting Against Trump Correlation=-0.44")+
scale_x_continuous(breaks=seq(0, 1, 0.05), limits=c(0.25, 0.75),labels = function(x){paste0(x*100, '%')})+# Add X axis scale from 0 to 1, break is 0.1. Since it is a rate, add % by using paste0()
scale_y_continuous(breaks=seq(0, 1, 0.02),limits=c(0.26, 0.50),labels = function(x){paste0(x*100, '%')})+# Add Y axis scale from 0 to 1, break is 0.1. Since it is a rate, add % by using paste0()
geom_smooth(method=lm,se=F)+ #add a tendency line
geom_text(aes(label = state.cat),na.rm = T)+#add label for the points we want to look at
theme(plot.title = element_text(hjust=0.5))+#put the title at the middle
theme_bw()+#add the theme
theme(panel.border=element_blank())
#Base on the graph, we could find that a higher Trump Support Rate comes with a lower saying discrimination prevalent. We can hava a negaive correlation here, which is -0.4386476
#state.select <- data$state[!c("CA","WY")]
data$state.cat<- factor(data$state,levels = c("CA","WY"),labels=c("California","Wyoming"))
#create a ggplot scatter plot based on X axis= discrim, Y axis = trump. We want to have a different size based on support ratem, and different transparent status based on discrim
G1<- ggplot(data, aes(y=discrim, x=trump))+geom_point(color="blue")
#Add more details
G1+xlab("Trump Support Rate")+# Add X axis names "Trump Support Rate"
ylab("Seeing Discriminated Prevalent")+# Add Y axis names "Seeing Discriminated Prevalent"
labs(title="Perceptions Of Discrimination Track Closely With Voting Against Trump Correlation=-0.44")+
scale_x_continuous(breaks=seq(0, 1, 0.05), limits=c(0.25, 0.75),labels = function(x){paste0(x*100, '%')})+# Add X axis scale from 0 to 1, break is 0.1. Since it is a rate, add % by using paste0()
scale_y_continuous(breaks=seq(0, 1, 0.02),limits=c(0.26, 0.50),labels = function(x){paste0(x*100, '%')})+# Add Y axis scale from 0 to 1, break is 0.1. Since it is a rate, add % by using paste0()
geom_smooth(method=lm,se=F)+ #add a tendency line
geom_text(aes(label = state.cat),na.rm = T)+#add label for the points we want to look at
theme(plot.title = element_text(hjust=0.5))+#put the title at the middle
theme_bw()+#add the theme
theme(panel.border=element_blank())
#Base on the graph, we could find that a higher Trump Support Rate comes with a lower saying discrimination prevalent. We can hava a negaive correlation here, which is -0.4386476
#state.select <- data$state[!c("CA","WY")]
data$state.cat<- factor(data$state,levels = c("CA","WY"),labels=c("California","Wyoming"))
#create a ggplot scatter plot based on X axis= discrim, Y axis = trump. We want to have a different size based on support ratem, and different transparent status based on discrim
G1<- ggplot(data, aes(y=discrim, x=trump))+geom_point(color="red")
#Add more details
G1+xlab("Trump Support Rate")+# Add X axis names "Trump Support Rate"
ylab("Seeing Discriminated Prevalent")+# Add Y axis names "Seeing Discriminated Prevalent"
labs(title="Perceptions Of Discrimination Track Closely With Voting Against Trump Correlation=-0.44")+
scale_x_continuous(breaks=seq(0, 1, 0.05), limits=c(0.25, 0.75),labels = function(x){paste0(x*100, '%')})+# Add X axis scale from 0 to 1, break is 0.1. Since it is a rate, add % by using paste0()
scale_y_continuous(breaks=seq(0, 1, 0.02),limits=c(0.26, 0.50),labels = function(x){paste0(x*100, '%')})+# Add Y axis scale from 0 to 1, break is 0.1. Since it is a rate, add % by using paste0()
geom_smooth(method=lm,se=F)+ #add a tendency line
geom_text(aes(label = state.cat),na.rm = T)+#add label for the points we want to look at
theme(plot.title = element_text(hjust=0.5))+#put the title at the middle
theme_bw()+#add the theme
theme(panel.border=element_blank())
#Base on the graph, we could find that a higher Trump Support Rate comes with a lower saying discrimination prevalent. We can hava a negaive correlation here, which is -0.4386476
#create a ggplot scatter plot based on X axis= discrim, Y axis = trump. We want to have a different size based on support ratem, and different transparent status based on discrim
G1<- ggplot(data, aes(y=discrim, x=trump))+geom_point(color="red_")
#Add more details
G1+xlab("Trump Support Rate")+# Add X axis names "Trump Support Rate"
ylab("Seeing Discriminated Prevalent")+# Add Y axis names "Seeing Discriminated Prevalent"
labs(title="Perceptions Of Discrimination Track Closely With Voting Against Trump Correlation=-0.44")+
scale_x_continuous(breaks=seq(0, 1, 0.05), limits=c(0.25, 0.75),labels = function(x){paste0(x*100, '%')})+# Add X axis scale from 0 to 1, break is 0.1.Limits are set here from 0.25 to 0.75. Since it is a rate, add % by using paste0()
scale_y_continuous(breaks=seq(0, 1, 0.02),limits=c(0.26, 0.50),labels = function(x){paste0(x*100, '%')})+# Add Y axis scale from 0 to 1, break is 0.1. Limits are set here from 0.26 to 0.50. Since it is a rate, add % by using paste0()
geom_smooth(method=lm,se=F)+ #add a tendency line
theme(plot.title = element_text(hjust=0.5))+#put the title at the middle
theme_bw()+#add the theme
theme(panel.border=element_blank())
#create a ggplot scatter plot based on X axis= discrim, Y axis = trump. We want to have a different size based on support ratem, and different transparent status based on discrim
G1<- ggplot(data, aes(y=discrim, x=trump))+geom_point(color="red")
#Add more details
G1+xlab("Trump Support Rate")+# Add X axis names "Trump Support Rate"
ylab("Seeing Discriminated Prevalent")+# Add Y axis names "Seeing Discriminated Prevalent"
labs(title="Perceptions Of Discrimination Track Closely With Voting Against Trump Correlation=-0.44")+
scale_x_continuous(breaks=seq(0, 1, 0.05), limits=c(0.25, 0.75),labels = function(x){paste0(x*100, '%')})+# Add X axis scale from 0 to 1, break is 0.1.Limits are set here from 0.25 to 0.75. Since it is a rate, add % by using paste0()
scale_y_continuous(breaks=seq(0, 1, 0.02),limits=c(0.26, 0.50),labels = function(x){paste0(x*100, '%')})+# Add Y axis scale from 0 to 1, break is 0.1. Limits are set here from 0.26 to 0.50. Since it is a rate, add % by using paste0()
geom_smooth(method=lm,se=F)+ #add a tendency line
theme(plot.title = element_text(hjust=0.5))+#put the title at the middle
theme_bw()+#add the theme
theme(panel.border=element_blank())
#Base on the graph, we could find that a higher Trump Support Rate comes with a lower saying discrimination prevalent. We can hava a negaive correlation here, which is -0.4386476
#create a ggplot scatter plot based on X axis= discrim, Y axis = trump. We want to have a different size based on support ratem, and different transparent status based on discrim
G1<- ggplot(data, aes(y=discrim, x=trump))+geom_point(color="red")
#Add more details
G1+xlab("Trump Support Rate")+# Add X axis names "Trump Support Rate"
ylab("Seeing Discriminated Prevalent")+# Add Y axis names "Seeing Discriminated Prevalent"
labs(title="Perceptions Of Discrimination Track Closely With Voting Against Trump Correlation=-0.44")+
scale_x_continuous(breaks=seq(0, 1, 0.05), limits=c(0.25, 0.75),labels = function(x){paste0(x*100, '%')})+# Add X axis scale from 0 to 1, break is 0.1. Limits here are from 0.25 to 0.75.Since it is a rate, add % by using paste0()
scale_y_continuous(breaks=seq(0, 1, 0.02),limits=c(0.26, 0.50),labels = function(x){paste0(x*100, '%')})+# Add Y axis scale from 0 to 1, break is 0.1. Limits here are from 0.26 to 0.50.Since it is a rate, add % by using paste0()
geom_smooth(method=lm,se=F)+ #add a tendency line
geom_text(aes(label = state.cat),na.rm = T)+#add label for the points we want to look at
theme(plot.title = element_text(hjust=0.5))+#put the title at the middle
theme_bw()+#add the theme
theme(panel.border=element_blank())
#Base on the graph, we could find that a higher Trump Support Rate comes with a lower saying discrimination prevalent. We can hava a negaive correlation here, which is -0.4386476
#create a ggplot scatter plot based on X axis= discrim, Y axis = trump. We want to have a different size based on support ratem, and different transparent status based on discrim
G1<- ggplot(data, aes(y=discrim, x=trump))+geom_point(color="red")
#Add more details
G1+xlab("Trump Support Rate")+# Add X axis names "Trump Support Rate"
ylab("Seeing Discriminated Prevalent")+# Add Y axis names "Seeing Discriminated Prevalent"
labs(title="Perceptions Of Discrimination Track Closely With Voting Against Trump Correlation=-0.44")+
scale_x_continuous(breaks=seq(0, 1, 0.05), limits=c(0.25, 0.75),labels = function(x){paste0(x*100, '%')})+# Add X axis scale from 0 to 1, break is 0.1. Limits here are from 0.25 to 0.75.Since it is a rate, add % by using paste0()
scale_y_continuous(breaks=seq(0, 1, 0.02),limits=c(0.26, 0.50),labels = function(x){paste0(x*100, '%')})+# Add Y axis scale from 0 to 1, break is 0.1. Limits here are from 0.26 to 0.50.Since it is a rate, add % by using paste0()
geom_smooth(method=lm,se=F)+ #add a tendency line
theme(plot.title = element_text(hjust=0.5))+#put the title at the middle
theme_bw()+#add the theme
theme(panel.border=element_blank())
#Base on the graph, we could find that a higher Trump Support Rate comes with a lower saying discrimination prevalent. We can hava a negaive correlation here, which is -0.4386476
#state.select <- data$state[!c("CA","WY")]
data$state.cat<- factor(data$state,levels = c("CA","WY"),labels=c("California","Wyoming"))
#create a ggplot scatter plot based on X axis= discrim, Y axis = trump. We want to have a different size based on support ratem, and different transparent status based on discrim
G1<- ggplot(data, aes(y=discrim, x=trump))+geom_point(aes(color=state, alpha= trump, size-discrim))
#Add more details
G1+xlab("Trump Support Rate")+# Add X axis names "Trump Support Rate"
ylab("Seeing Discriminated Prevalent")+# Add Y axis names "Seeing Discriminated Prevalent"
labs(title="Perceptions Of Discrimination Track Closely With Voting Against Trump Correlation=-0.44")+
scale_x_continuous(breaks=seq(0, 1, 0.05), limits=c(0.25, 0.75),labels = function(x){paste0(x*100, '%')})+# Add X axis scale from 0 to 1, break is 0.1. Limits here are from 0.25 to 0.75.Since it is a rate, add % by using paste0()
scale_y_continuous(breaks=seq(0, 1, 0.02),limits=c(0.26, 0.50),labels = function(x){paste0(x*100, '%')})+# Add Y axis scale from 0 to 1, break is 0.1. Limits here are from 0.26 to 0.50.Since it is a rate, add % by using paste0()
geom_smooth(method=lm,se=F)+ #add a tendency line
theme(plot.title = element_text(hjust=0.5))+#put the title at the middle
theme_bw()+#add the theme
theme(panel.border=element_blank())
#state.select <- data$state[!c("CA","WY")]
data$state.cat<- factor(data$state,levels = c("CA","WY"),labels=c("California","Wyoming"))
#create a ggplot scatter plot based on X axis= discrim, Y axis = trump. We want to have a different size based on support ratem, and different transparent status based on discrim
G1<- ggplot(data, aes(y=discrim, x=trump))+geom_point(aes(color=state, alpha= trump, size=discrim))
#Add more details
G1+xlab("Trump Support Rate")+# Add X axis names "Trump Support Rate"
ylab("Seeing Discriminated Prevalent")+# Add Y axis names "Seeing Discriminated Prevalent"
labs(title="Perceptions Of Discrimination Track Closely With Voting Against Trump Correlation=-0.44")+
scale_x_continuous(breaks=seq(0, 1, 0.05), limits=c(0.25, 0.75),labels = function(x){paste0(x*100, '%')})+# Add X axis scale from 0 to 1, break is 0.1. Limits here are from 0.25 to 0.75.Since it is a rate, add % by using paste0()
scale_y_continuous(breaks=seq(0, 1, 0.02),limits=c(0.26, 0.50),labels = function(x){paste0(x*100, '%')})+# Add Y axis scale from 0 to 1, break is 0.1. Limits here are from 0.26 to 0.50.Since it is a rate, add % by using paste0()
geom_smooth(method=lm,se=F)+ #add a tendency line
theme(plot.title = element_text(hjust=0.5))+#put the title at the middle
theme_bw()+#add the theme
theme(panel.border=element_blank())
#Base on the graph, cannot say the graph is not helpful for defying the relationship between the sweeing Discriminated Prevalent and Trump Support Rate.Becased the tendency lne and corrolation here is based on the point in the limited part. We cannot have the relationship baed on a baised sample.
#state.select <- data$state[!c("CA","WY")]
data$state.cat<- factor(data$state,levels = c("CA","WY"),labels=c("California","Wyoming"))
#create a ggplot scatter plot based on X axis= discrim, Y axis = trump. We want to have a different size based on support ratem, and different transparent status based on discrim
G1<- ggplot(data, aes(y=discrim, x=trump))+geom_point(aes(color=state, alpha= trump, size=discrim))
#Add more details
G1+xlab("Trump Support Rate")+# Add X axis names "Trump Support Rate"
ylab("Seeing Discriminated Prevalent")+# Add Y axis names "Seeing Discriminated Prevalent"
labs(title="Perceptions Of Discrimination Track Closely With Voting Against Trump Correlation=-0.44")+
scale_x_continuous(breaks=seq(0, 1, 0.05),labels = function(x){paste0(x*100, '%')})+# Add X axis scale from 0 to 1, break is 0.1. Since it is a rate, add % by using paste0()
scale_y_continuous(breaks=seq(0, 1, 0.05),labels = function(x){paste0(x*100, '%')})+# Add Y axis scale from 0 to 1, break is 0.1. Limits here are from 0.26 to 0.50.Since it is a rate, add % by using paste0()
geom_smooth(method=lm,se=F)+ #add a tendency line
theme(plot.title = element_text(hjust=0.5))+#put the title at the middle
theme_bw()+#add the theme
theme(panel.border=element_blank())
#Base on the graph, cannot say the graph is not helpful for defying the relationship between the sweeing Discriminated Prevalent and Trump Support Rate.Becased the tendency lne and corrolation here is based on the point in the limited part. We cannot have the relationship baed on a baised sample.
#state.select <- data$state[!c("CA","WY")]
data$state.cat<- factor(data$state,levels = c("CA","WY"),labels=c("California","Wyoming"))
#create a ggplot scatter plot based on X axis= discrim, Y axis = trump. We want to have a different size based on support ratem, and different transparent status based on discrim
G1<- ggplot(data, aes(y=discrim, x=trump))+geom_point(aes(color=state, alpha= trump, size=discrim))
#Add more details
G1+xlab("Trump Support Rate")+# Add X axis names "Trump Support Rate"
ylab("Seeing Discriminated Prevalent")+# Add Y axis names "Seeing Discriminated Prevalent"
labs(title="Perceptions Of Discrimination Track Closely With Voting Against Trump Correlation=-0.44")+
scale_x_continuous(breaks=seq(0, 1, 0.05),labels = function(x){paste0(x*100, '%')})+# Add X axis scale from 0 to 1, break is 0.1. Since it is a rate, add % by using paste0()
scale_y_continuous(breaks=seq(0, 1, 0.05),labels = function(x){paste0(x*100, '%')})+# Add Y axis scale from 0 to 1, break is 0.1. Since it is a rate, add % by using paste0()
geom_smooth(method=lm,se=F)+ #add a tendency line
geom_text(text=state.cat)
#state.select <- data$state[!c("CA","WY")]
data$state.cat<- factor(data$state,levels = c("CA","WY"),labels=c("California","Wyoming"))
#create a ggplot scatter plot based on X axis= discrim, Y axis = trump. We want to have a different size based on support ratem, and different transparent status based on discrim
G1<- ggplot(data, aes(y=discrim, x=trump))+geom_point(aes(color=state, alpha= trump, size=discrim))
#Add more details
G1+xlab("Trump Support Rate")+# Add X axis names "Trump Support Rate"
ylab("Seeing Discriminated Prevalent")+# Add Y axis names "Seeing Discriminated Prevalent"
labs(title="Perceptions Of Discrimination Track Closely With Voting Against Trump Correlation=-0.44")+
scale_x_continuous(breaks=seq(0, 1, 0.05),labels = function(x){paste0(x*100, '%')})+# Add X axis scale from 0 to 1, break is 0.1. Since it is a rate, add % by using paste0()
scale_y_continuous(breaks=seq(0, 1, 0.05),labels = function(x){paste0(x*100, '%')})+# Add Y axis scale from 0 to 1, break is 0.1. Since it is a rate, add % by using paste0()
geom_smooth(method=lm,se=F)+ #add a tendency line
geom_text(aes(text=state.cat))+
theme(plot.title = element_text(hjust=0.5))+#put the title at the middle
theme_bw()+#add the theme
theme(panel.border=element_blank())
#state.select <- data$state[!c("CA","WY")]
data$state.cat<- factor(data$state,levels = c("CA","WY"),labels=c("California","Wyoming"))
#create a ggplot scatter plot based on X axis= discrim, Y axis = trump. We want to have a different size based on support ratem, and different transparent status based on discrim
G1<- ggplot(data, aes(y=discrim, x=trump))+geom_point(aes(color=state, alpha= trump, size=discrim))
#Add more details
G1+xlab("Trump Support Rate")+# Add X axis names "Trump Support Rate"
ylab("Seeing Discriminated Prevalent")+# Add Y axis names "Seeing Discriminated Prevalent"
labs(title="Perceptions Of Discrimination Track Closely With Voting Against Trump Correlation=-0.44")+
scale_x_continuous(breaks=seq(0, 1, 0.05),labels = function(x){paste0(x*100, '%')})+# Add X axis scale from 0 to 1, break is 0.1. Since it is a rate, add % by using paste0()
scale_y_continuous(breaks=seq(0, 1, 0.05),labels = function(x){paste0(x*100, '%')})+# Add Y axis scale from 0 to 1, break is 0.1. Since it is a rate, add % by using paste0()
geom_smooth(method=lm,se=F)+ #add a tendency line
geom_text(aes(label=state.cat))+
theme(plot.title = element_text(hjust=0.5))+#put the title at the middle
theme_bw()+#add the theme
theme(panel.border=element_blank())
#Base on the graph, cannot say the graph is not helpful for defying the relationship between the sweeing Discriminated Prevalent and Trump Support Rate.Becased the tendency lne and corrolation here is based on the point in the limited part. We cannot have the relationship baed on a baised sample.
#state.select <- data$state[!c("CA","WY")]
data$state.cat<- factor(data$state,levels = c("CA","WY"),labels=c("California","Wyoming"))
#create a ggplot scatter plot based on X axis= discrim, Y axis = trump. We want to have a different size based on support ratem, and different transparent status based on discrim
G1<- ggplot(data, aes(y=discrim, x=trump))+geom_point(aes(color=state, alpha= trump, size=discrim))
#Add more details
G1+xlab("Trump Support Rate")+# Add X axis names "Trump Support Rate"
ylab("Seeing Discriminated Prevalent")+# Add Y axis names "Seeing Discriminated Prevalent"
labs(title="Perceptions Of Discrimination Track Closely With Voting Against Trump Correlation=-0.44")+
scale_x_continuous(breaks=seq(0, 1, 0.05),labels = function(x){paste0(x*100, '%')})+# Add X axis scale from 0 to 1, break is 0.1. Since it is a rate, add % by using paste0()
scale_y_continuous(breaks=seq(0, 1, 0.05),labels = function(x){paste0(x*100, '%')})+# Add Y axis scale from 0 to 1, break is 0.1. Since it is a rate, add % by using paste0()
geom_smooth(method=lm,se=F)+ #add a tendency line
geom_text(aes(label=state.cat))+
guides(fill=FALSE)+
theme(plot.title = element_text(hjust=0.5))+#put the title at the middle
theme_bw()+#add the theme
theme(panel.border=element_blank())
#Base on the graph, as we have find all the point, were are able to say there is there a more clear realationship between seeing discriminated prevalent and Trump support rate.
#state.select <- data$state[!c("CA","WY")]
data$state.cat<- factor(data$state,levels = c("CA","WY"),labels=c("California","Wyoming"))
#create a ggplot scatter plot based on X axis= discrim, Y axis = trump. We want to have a different size based on support ratem, and different transparent status based on discrim
G1<- ggplot(data, aes(y=discrim, x=trump))+geom_point(aes(color=state, alpha= trump, size=discrim))
#Add more details
G1+xlab("Trump Support Rate")+# Add X axis names "Trump Support Rate"
ylab("Seeing Discriminated Prevalent")+# Add Y axis names "Seeing Discriminated Prevalent"
labs(title="Perceptions Of Discrimination Track Closely With Voting Against Trump Correlation=-0.44")+
scale_x_continuous(breaks=seq(0, 1, 0.05),labels = function(x){paste0(x*100, '%')})+# Add X axis scale from 0 to 1, break is 0.1. Since it is a rate, add % by using paste0()
scale_y_continuous(breaks=seq(0, 1, 0.05),labels = function(x){paste0(x*100, '%')})+# Add Y axis scale from 0 to 1, break is 0.1. Since it is a rate, add % by using paste0()
geom_smooth(method=lm,se=F)+ #add a tendency line
geom_text(aes(label=state.cat))+
guides(color=FALSE)+
theme(plot.title = element_text(hjust=0.5))+#put the title at the middle
theme_bw()+#add the theme
theme(panel.border=element_blank())
#Base on the graph, as we have find all the point, were are able to say there is there a more clear realationship between seeing discriminated prevalent and Trump support rate.
#state.select <- data$state[!c("CA","WY")]
data$state.cat<- factor(data$state,levels = c("CA","WY"),labels=c("California","Wyoming"))
#create a ggplot scatter plot based on X axis= discrim, Y axis = trump. We want to have a different size based on support ratem, and different transparent status based on discrim
G1<- ggplot(data, aes(y=discrim, x=trump))+geom_point(aes(color=state, alpha= trump, size=discrim))
#Add more details
G1+xlab("Trump Support Rate")+# Add X axis names "Trump Support Rate"
ylab("Seeing Discriminated Prevalent")+# Add Y axis names "Seeing Discriminated Prevalent"
labs(title="Perceptions Of Discrimination Track Closely With Voting Against Trump Correlation=-0.44")+
scale_x_continuous(breaks=seq(0, 1, 0.05),labels = function(x){paste0(x*100, '%')})+# Add X axis scale from 0 to 1, break is 0.1. Since it is a rate, add % by using paste0()
scale_y_continuous(breaks=seq(0, 1, 0.05),labels = function(x){paste0(x*100, '%')})+# Add Y axis scale from 0 to 1, break is 0.1. Since it is a rate, add % by using paste0()
geom_smooth(method=lm,se=F)+ #add a tendency line
geom_text(aes(label=state.cat))+
guides(color=FALSE, size=F, alpha=F)+
theme(plot.title = element_text(hjust=0.5))+#put the title at the middle
theme_bw()+#add the theme
theme(panel.border=element_blank())
#Base on the graph, as we have find all the point, were are able to say there is there a more clear realationship between seeing discriminated prevalent and Trump support rate.
knitr::opts_chunk$set(echo = TRUE)
Pb<- 0.55
Py<- 1-Pb
Brave_Win_Prob<-
dnbinom(0,4,Pb)+
dnbinom(1,4,Pb)+
dnbinom(2,4,Pb)+
dnbinom(3,4,Pb)
Brave_Win_Prob
Brave_Win_Prob<- function(p){
pnbinom(3,4,p)
}
Pbseries <- seq(0.5,1, 0.01)
B_win_Prob <- rep(NA,length(Pbseries))
for(i in 1:length(Pbseries)){
B_win_Prob[i]<-
dnbinom(0,4,Pbseries[i])+
dnbinom(1,4,Pbseries[i])+
dnbinom(2,4,Pbseries[i])+
dnbinom(3,4,Pbseries[i])
}
Pbseries
B_win_Prob
data<-data.frame(Pbseries,B_win_Prob)
data
require(ggplot2)
graph_question2<-ggplot(data,aes(x=Pbseries,y=B_win_Prob))+geom_point()
graph_question2+labs(
titles="Probability of winning the World Series",
x="Probability of the Braves winning a head to head matchup",
y="P(Braves) win World Series")+
theme_bw()+
theme(plot.title=element_text(hjust=0.5))
series_length <- seq(1, 99999, 2)
for(i in 1:length(series_length)){
Pb_win<-pnbinom(ceiling(series_length[i]/2)-1,ceiling(series_length[i]/2),0.55)
if(Pb_win>=0.8){
shortest<-series_length[i]
p_shortest<-Pb_win
break
}
}
shortest
p_shortest
Pbseries <- seq(0.5,1, 0.01)
Shortest_Record<- rep(NA,length(Pbseries))
for(i in 1:length(Pbseries)){
for(j in 1:length(series_length)){
Pb_win<-pnbinom(ceiling(series_length[j]/2)-1,ceiling(series_length[j]/2),Pbseries[i])
if(Pb_win>=0.8){
shortest<-series_length[j]
break}
}
Shortest_Record[i]<-shortest
}
Pbseries
Shortest_Record
data2<-data.frame(Pbseries,Shortest_Record)
data2
graph_question4<-ggplot(data2,aes(x=Pbseries,y=Shortest_Record))+geom_point()
graph_question4+labs(
titles="Shortest series so that P(Win WS given p)>=0.8",
x="Probability of the Braves winning a head to head matchup",
y="Series length")+
theme_bw()+
theme(plot.title=element_text(hjust=0.5))
graph_question4<-ggplot(data2,aes(x=Pbseries,y=Shortest_Record))+geom_point()
graph_question4+labs(
titles="Shortest series so that P(Win WS given p)>=0.8",
x="Probability of the Braves winning a head to head matchup",
y="Series length")+
theme_bw()+
theme(plot.title=element_text(hjust=0.5))
prob_Braves_win_in_games<-(dnbinom(3,4,0.45)+dnbinom(3,4,0.55))/2
prob_Braves_win_in_games_Pb<-dnbinom(3,4,0.55)
prob_Braves_win_in_games
prob_Braves_win_in_games_Pb<-dnbinom(3,4,0.55)
prob_Braves_win_in_games_Pb<-dnbinom(3,4,0.55)
prob_Braves_win_in_games_Pb
<p>&emsp; ThusP(PB=0.55|Braves win World Series in 7 games) will be P(Braves win World Series in 7 games|PB=0.55) * P(PB=0.55) ÷ P(Braves win World Series in 7 games).</p>
prob_Braves_win_in_games_Pb
prob_Braves_win_in_games_Pb*0.5/prob_Braves_win_in_games
